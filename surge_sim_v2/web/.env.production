# 本番ビルド（FastAPI が配信）では接続先を指定しない。
# 空にすると、開いているページのホストから WS/API の接続先を実行時に導出する。
# → http://localhost:8000（sim）でも http://<raspi-ip>:8000（実機）でも自動で正しく繋がる。
#
# 別ホストのサーバーに固定で繋ぎたい場合のみ、明示的に指定する：
# VITE_WS_URL=ws://192.168.1.10:8000/ws
VITE_WS_URL=
