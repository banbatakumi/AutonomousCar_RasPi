# ml/ — Mac側オフラインMLパイプラインの依存（`raspi/requirements.txt` とは別管理）
#
#   .venv/bin/pip install -r ml/requirements.txt
#
# `sim/`（シミュレータ）・`gui/`（React）と同じく、Pi以外で動くコードの依存は
# raspi/ 側に持ち込まない。torch/opencv/segment-anything を Pi に入れる必要は無い
# （Pi 側は `raspi/nodes/cam_perception_node.py` が onnxruntime だけで推論する）。

torch>=2.2
torchvision>=0.17

# ONNX エクスポート・推論側との往復検証（`raspi/requirements.txt` の
# onnxruntime と同じ理由で入れておく。Mac 側でも parity 確認に使う）
onnx>=1.16
onnxruntime>=1.18,<2
onnxscript>=0.7          # torch 2.5+ の既定エクスポータ（dynamo=True）が要求する

# アノテーションツール（`ml/annotate.py`）の表示・マウス入力
opencv-python>=4.9

# SAM（Segment Anything）— 半自動アノテーションの本体。
# チェックポイントは同梱されないので別途ダウンロードすること:
#   https://dl.fbaipublicfiles.com/segment_anything/sam_vit_b_01ec64.pth
segment-anything==1.0

pillow>=10
numpy>=2.1

# `.mcap` からのフレーム抽出（`ml/extract_frames.py`）。raspi 側と同じライブラリ
mcap>=1.2,<2
