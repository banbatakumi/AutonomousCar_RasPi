# システム同定ツール（Mac側、tools/sysid/） — Piには入れない
#
#   .venv/bin/pip install -r tools/requirements.txt
#   .venv/bin/python -m tools.sysid.gui

mcap>=1.2,<2            # mcapログの読み込み（raspi/requirements.txt と同じピン留め）
numpy                    # ステップ応答のフィッティング（raspi/requirements.txt に既に入っている想定）
tomlkit                  # vehicle.toml のコメント・書式を保ったまま書き換える
