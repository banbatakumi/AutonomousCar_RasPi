"""デフォルト長方形コース。

外周 6m(x) × 4m(y)、内周 4m × 2m、コース幅 1m の周回コース。
壁は線分のリスト: [((x1, y1), (x2, y2)), ...]
スタートは下側ストレートの左寄り、East(+x)向き。
"""

COURSE_NAME = "Default Rectangle"

# 外周（左下原点）
_OUTER = [
    ((0.0, 0.0), (6.0, 0.0)),   # 下
    ((6.0, 0.0), (6.0, 4.0)),   # 右
    ((6.0, 4.0), (0.0, 4.0)),   # 上
    ((0.0, 4.0), (0.0, 0.0)),   # 左
]

# 内周（コース幅1mなので外周から1m内側 → 4m×2mの島）
_INNER = [
    ((1.0, 1.0), (5.0, 1.0)),   # 下
    ((5.0, 1.0), (5.0, 3.0)),   # 右
    ((5.0, 3.0), (1.0, 3.0)),   # 上
    ((1.0, 3.0), (1.0, 1.0)),   # 左
]

WALLS = _OUTER + _INNER

# 下側ストレート中央（y=0〜1の通路の中心 y=0.5）、East向き
START_POSE = (3.0, 0.5, 0.0)  # x[m], y[m], heading[deg]

# コース中心線（経路追従用のウェイポイント、反時計回りの閉ループ）
# 外周から0.5m内側 = コース幅1mの中央を通る矩形ループ
CENTER_LINE = [
    (0.5, 0.5),
    (5.5, 0.5),
    (5.5, 3.5),
    (0.5, 3.5),
]
